:navigation-title: Reference

..  _reference:

=========
Reference
=========

The reference describes the contracts a project and a script can rely on:
what every operation does, what ``.ddev/branchery.yaml`` accepts, and how the
same operations are reached from a script or through the REST API.

..  grid::

    ..  card:: :doc:`/reference/operations`
        :action: Read it

        Every command, and what each operation does step by step.

    ..  card:: :doc:`/reference/configuration`
        :action: Read it

        Every key of the project's file: profiles, lifecycle moments, runtimes,
        carried files, data and links.

    ..  card:: :doc:`/reference/automation`
        :action: Read it

        JSON output, detached jobs and the REST API.

..  toctree::
    :hidden:

    reference/operations
    reference/configuration
    reference/automation
