/**
 * The editor the profiles create when they install an application. The same one
 * every time and written down in the documentation, which is what makes it a
 * development login and not a secret. Here once rather than in each of the two
 * places that show it.
 */
export const EDITOR = { user: 'admin', password: 'Password1!' };
